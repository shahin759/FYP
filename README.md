This application is intended to run only on the owner's device. It requires sensitive credentials such as API keys, security keys, and email (mail) credentials to function properly.

For security reasons, these credentials are not included in the repository and should never be shared publicly.
